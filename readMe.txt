*] we make node as static because we can use it without creating the object of the outer class 
*] we don't make as static sub clss we have to create object of the iuter class and then we make the object of the inner class inside the  
